<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mww="using:WasabiCli.Models.WalletWasabi"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="WasabiCli.Views.WalletWasabi.AccountInfoView"
             x:CompileBindings="True" x:DataType="mww:AccountInfo">
  <ScrollViewer HorizontalScrollBarVisibility="Disabled">
    <StackPanel Spacing="4">
      <!-- Name -->
      <Label Content="Name" />
      <TextBox Text="{Binding Name}"
               IsReadOnly="True" />
      <!-- PublicKey -->
      <Label Content="PublicKey" />
      <TextBox Text="{Binding PublicKey}"
               IsReadOnly="True" />
      <!-- KeyPath -->
      <Label Content="KeyPath" />
      <TextBox Text="{Binding KeyPath}"
               IsReadOnly="True" />
    </StackPanel>
  </ScrollViewer>
</UserControl>

