<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:WasabiCli.ViewModels"
        xmlns:controls="using:WasabiCli.Controls"
        xmlns:views="using:WasabiCli.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="670"
        x:Class="WasabiCli.Views.MainWindow"
        x:CompileBindings="True" x:DataType="vm:MainWindowViewModel"
        WindowStartupLocation="CenterScreen"
        Width="400"
        Height="670"
        Icon="/Assets/avalonia-logo.ico"
        Title="Wasabi.Wallet RPC">
  <Design.DataContext>
    <vm:MainWindowViewModel>
      <x:Arguments>
        <vm:NavigationServiceViewModel />
      </x:Arguments>
    </vm:MainWindowViewModel>
  </Design.DataContext>
  <Panel>
    <controls:NavigateBackControl Header="Wasabi.Wallet RPC"
                                  EnableBack="False"
                                  IsVisible="{Binding NavigationService.CurrentDialog, Converter={x:Static ObjectConverters.IsNull}}">
      <views:HomeView />
    </controls:NavigateBackControl>
    <views:NavigationServiceView DataContext="{Binding NavigationService}" />
  </Panel>
</Window>
